const Results = () => {
    // TODO: pass userAnswers state/prop (?) from Question component
    
    // TODO: iterate through car objects, return cards for objects that match user's selection
    
    return (
        <>
            <p>Results yay</p>
        </>
    );
};

export default Results;
